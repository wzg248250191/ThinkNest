# 项目规则（自动提醒）

1. 对话语言：所有说明与回答一律使用中文（代码、文件名、命令保持原样）。
2. 系统环境：Windows。
3. 架构约定：Flutter 项目使用 GetX 框架。
4. 组件约定：Row 使用 `<Widget>[].toRow()` 创建；Column 使用 `<Widget>[].toColumn()` 创建。
5. 注释规范（新增）：你写的代码若新增方法/函数，必须为每个方法/函数添加中文注释，说明“该方法做什么”。
6. 注释规范（修改）：你修改的代码行若涉及关键逻辑/关键分支/异步或性能敏感点，必须补充中文注释说明原因与意图。
7. 注释规范（例外）：输出信息无需添加注释，例如 `print`、`debugPrint`、`DebugUtils.log`。
